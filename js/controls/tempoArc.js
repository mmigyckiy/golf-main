export const TEMPO_ARC = {
  arcStart: Math.PI * 0.85,
  arcEnd: Math.PI * 2.15,
  motionStart: Math.PI * 1.05,
  motionEnd: Math.PI * 1.95,
  targetAngle: Math.PI * 1.5
};

Object.freeze(TEMPO_ARC);
